name: Validate and deploy shared infrastructure

on:
  push:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: shared-infrastructure-${{ github.ref }}
  cancel-in-progress: false

env:
  AWS_REGION: ${{ vars.AWS_REGION }}
  CDK_CLI_VERSION: 2.1139.0

jobs:
  cdk-test:
    name: cdk test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Run tests
        run: uv run pytest -q

  cdk-synth:
    name: cdk synth
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install pinned CDK CLI
        run: npm install --global "aws-cdk@$CDK_CLI_VERSION"

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Synthesize and print templates
        run: |
          cdk synth SharedNetworkStack --no-lookups
          cdk synth SharedHostedZonesStack --no-lookups
          cdk synth SharedCertificatesStack --no-lookups
          cdk synth SharedDomainsStack --no-lookups
          cdk synth SharedInfrastructureStack --no-lookups

  cdk-diff:
    name: cdk diff
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install pinned CDK CLI
        run: npm install --global "aws-cdk@$CDK_CLI_VERSION"

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Print CDK diffs without creating change sets
        run: |
          cdk diff SharedNetworkStack --change-set=false --no-lookups
          cdk diff SharedHostedZonesStack --change-set=false --no-lookups
          cdk diff SharedCertificatesStack --change-set=false --no-lookups
          cdk diff SharedDomainsStack --change-set=false --no-lookups
          cdk diff SharedInfrastructureStack --change-set=false --no-lookups

  cdk-deploy:
    name: cdk deploy
    needs: [cdk-test, cdk-synth, cdk-diff]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: production

    steps:
      - uses: actions/checkout@v5

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          cache-dependency-glob: uv.lock

      - name: Install pinned CDK CLI
        run: npm install --global "aws-cdk@$CDK_CLI_VERSION"

      - name: Install dependencies
        run: uv sync --frozen --group dev

      - name: Deploy shared stacks
        run: |
          cdk deploy SharedNetworkStack --exclusively --revert-drift --require-approval never
          cdk deploy SharedHostedZonesStack --exclusively --revert-drift --require-approval never
          cdk deploy SharedCertificatesStack --exclusively --revert-drift --require-approval never
          cdk deploy SharedDomainsStack --exclusively --revert-drift --require-approval never
          cdk deploy SharedInfrastructureStack --exclusively --revert-drift --require-approval never
